<style>
/* 1. FIX VERTICAL SPACING AND OVERLAP (The problem from your screenshot) */
.article-page-container {
    /* Use a large value (130px) to push content down past the red bar and navigation bar. 
       You may need to slightly adjust this number (e.g., 100px or 150px) based on your final header height. */
    padding-top: 130px; 
    padding-bottom: 50px; 
}

/* 2. FIX READABILITY (Text Color) */
.article-content-wrapper h1,
.article-content-wrapper .article-meta,
.article-content-wrapper .article-meta small,
.article-content-wrapper .article-meta time,
.article-content-wrapper .article-body,
.article-content-wrapper .article-body p {
    /* Set text to a dark, readable color against the light background */
    color: #333333 !important; 
    /* The !important tag is added temporarily to ensure this style overrides any faint text styles from your theme. */
}

/* 3. BASIC ARTICLE STYLING */
.article-body {
    /* Ensures body text is legible */
    line-height: 1.6; 
    font-size: 16px; 
}
</style>


<div class="page-width article-page-container">

    <div class="article-content-wrapper">

        <h1>{{ article.title }}</h1>

        <div class="article-meta">
            <small>{{ article.author }}</small>
            <time datetime="{{ article.published_at | date: '%Y-%m-%d' }}">{{ article.published_at | date: '%Y-%m-%d' }}</time>
        </div>

        {% if article.image %}
            <img
                src="{{ article.image | image_url: width: article.image.width, height: article.image.height }}"
                alt="{{ article.image.alt }}"
                class="article-image"
                width="{{ article.image.width }}"
                height="{{ article.image.height }}"
                loading="lazy"
            >
        {% endif %}
        
        <div class="article-body">
            {{ article.content }}
        </div>

    </div>
    
</div>